window.BigOSAppAPI.register({    id: 'cookie_clicker',    name: 'Ciasteczkowy Klikacz',    icon: '🍪',    width: '400px',    height: 'auto',    html: `        <div class="flex flex-col gap-4 p-4 items-center g-bg rounded-lg">            <div class="text-center select-none">                <h2 class="g-text text-3xl font-extrabold tracking-wide" id="cookie_count">0 ciasteczek</h2>                <p class="g-text-muted text-sm mt-1" id="cookie_cps">na sekundę (CPS): 0</p>            </div>                        <div class="relative my-4 flex items-center justify-center">                <button onclick="window.BigCookieClicker.clickCookie()" class="focus:outline-none transform active:scale-90 hover:scale-105 transition-all duration-100 select-none cursor-pointer">                    <span class="text-9xl filter drop-shadow-lg block">🍪</span>                </button>            </div>                        <div class="w-full g-panel p-3 rounded-xl border g-border flex flex-col gap-2">                <h3 class="g-text font-bold text-sm border-b g-border pb-1.5 mb-1 flex justify-between">                    <span>Sklep z ulepszeniami</span>                    <span class="g-text-muted text-xs font-normal">Posiadane</span>                </h3>                                <button onclick="window.BigCookieClicker.buyClicker()" id="btn_clicker" class="g-btn flex justify-between items-center p-2.5 rounded-lg text-sm w-full transition-all duration-150">                    <span class="font-medium">👆 Auto-Klikacz (+0.1 CPS)</span>                    <span class="font-bold text-xs px-2 py-1 rounded g-bg" id="cost_clicker">15 🍪</span>                </button>                                <button onclick="window.BigCookieClicker.buyGrandma()" id="btn_grandma" class="g-btn flex justify-between items-center p-2.5 rounded-lg text-sm w-full transition-all duration-150">                    <span class="font-medium">👵 Babcia (+1.0 CPS)</span>                    <span class="font-bold text-xs px-2 py-1 rounded g-bg" id="cost_grandma">100 🍪</span>                </button>                                <button onclick="window.BigCookieClicker.buyFactory()" id="btn_factory" class="g-btn flex justify-between items-center p-2.5 rounded-lg text-sm w-full transition-all duration-150">                    <span class="font-medium">🏭 Fabryka (+8.0 CPS)</span>                    <span class="font-bold text-xs px-2 py-1 rounded g-bg" id="cost_factory">1100 🍪</span>                </button>                                <button onclick="window.BigCookieClicker.buyMine()" id="btn_mine" class="g-btn flex justify-between items-center p-2.5 rounded-lg text-sm w-full transition-all duration-150">                    <span class="font-medium">🚀 Kopalnia (+47.0 CPS)</span>                    <span class="font-bold text-xs px-2 py-1 rounded g-bg" id="cost_mine">12000 🍪</span>                </button>            </div>                        <div class="w-full flex justify-between text-xs g-text-muted px-1 mt-1 border-t g-border pt-2">                <span id="stat_clicks">Kliknięcia: 0</span>                <span id="stat_owned">Ulepszenia: 0</span>            </div>        </div>    `,    globalName: 'BigCookieClicker',    global: {        state: {            cookies: 0,            totalClicks: 0,            upgradesOwned: 0,            upgrades: {                clicker: { cost: 15, cps: 0.1, count: 0 },                grandma: { cost: 100, cps: 1.0, count: 0 },                factory: { cost: 1100, cps: 8.0, count: 0 },                mine: { cost: 12000, cps: 47.0, count: 0 }            }        },        clickCookie: () => {            const self = window.BigCookieClicker;            self.state.cookies += 1;            self.state.totalClicks += 1;            self.updateUI();        },        buyClicker: () => {            window.BigCookieClicker.buyUpgrade('clicker');        },        buyGrandma: () => {            window.BigCookieClicker.buyUpgrade('grandma');        },        buyFactory: () => {            window.BigCookieClicker.buyUpgrade('factory');        },        buyMine: () => {            window.BigCookieClicker.buyUpgrade('mine');        },        buyUpgrade: (type) => {            const self = window.BigCookieClicker;            const upgrade = self.state.upgrades[type];            if (self.state.cookies >= upgrade.cost) {                self.state.cookies -= upgrade.cost;                upgrade.count += 1;                self.state.upgradesOwned += 1;                upgrade.cost = Math.floor(upgrade.cost * 1.15);                self.updateUI();            }        },        getCPS: () => {            const self = window.BigCookieClicker;            let totalCPS = 0;            for (const key in self.state.upgrades) {                totalCPS += self.state.upgrades[key].count * self.state.upgrades[key].cps;            }            return parseFloat(totalCPS.toFixed(1));        },        updateUI: () => {            const self = window.BigCookieClicker;            const elCookies = document.getElementById('cookie_count');            if (!elCookies) return;                        const displayCookies = Math.floor(self.state.cookies);            elCookies.innerText = displayCookies + (displayCookies === 1 ? ' ciasteczko' : ' ciasteczek');                        const cps = self.getCPS();            document.getElementById('cookie_cps').innerText = 'na sekundę (CPS): ' + cps.toFixed(1);                        for (const key in self.state.upgrades) {                const upgrade = self.state.upgrades[key];                const btn = document.getElementById('btn_' + key);                const costEl = document.getElementById('cost_' + key);                                if (costEl) {                    costEl.innerText = upgrade.cost + ' 🍪 [x' + upgrade.count + ']';                }                                if (btn) {                    if (self.state.cookies >= upgrade.cost) {                        btn.style.opacity = '1';                        btn.style.cursor = 'pointer';                    } else {                        btn.style.opacity = '0.5';                        btn.style.cursor = 'not-allowed';                    }                }            }                        const elClicks = document.getElementById('stat_clicks');            if (elClicks) elClicks.innerText = 'Kliknięcia: ' + self.state.totalClicks;                        const elOwned = document.getElementById('stat_owned');            if (elOwned) elOwned.innerText = 'Ulepszenia: ' + self.state.upgradesOwned;        },        tick: () => {            const self = window.BigCookieClicker;            const cps = self.getCPS();            if (cps > 0) {                self.state.cookies += cps / 10;                self.updateUI();            }        }    },    init: () => {        if (window.BigCookieClickerInterval) {            clearInterval(window.BigCookieClickerInterval);        }                window.BigCookieClickerInterval = setInterval(() => {            if (document.getElementById('cookie_count')) {                window.BigCookieClicker.tick();            } else {                clearInterval(window.BigCookieClickerInterval);            }        }, 100);                window.BigCookieClicker.updateUI();    }});