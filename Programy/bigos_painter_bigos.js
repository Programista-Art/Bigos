window.BigOSAppAPI.register({    id: 'bigos_painter',    name: 'BigPaint Studio',    icon: '🎨',    width: '800px',    height: '600px',    html: `        <div class="flex flex-col gap-2 h-full">            <div class="flex gap-2 items-center g-bg p-2 rounded">                <input type="color" id="paint_color" class="w-10 h-10 cursor-pointer">                <input type="range" id="paint_size" min="1" max="50" value="5" class="w-24">                <button onclick="window.BigPaint.clear()" class="g-btn px-3 py-1">Wyczyść</button>                <button onclick="window.BigPaint.save()" class="g-btn px-3 py-1">Zapisz JPG</button>            </div>            <canvas id="paint_canvas" class="g-bg g-border rounded cursor-crosshair w-full flex-grow" style="background: white;"></canvas>        </div>    `,    globalName: 'BigPaint',    global: {        clear: () => {            const canvas = document.getElementById('paint_canvas');            const ctx = canvas.getContext('2d');            ctx.clearRect(0, 0, canvas.width, canvas.height);        },        save: () => {            const canvas = document.getElementById('paint_canvas');            const link = document.createElement('a');            link.download = 'obraz.jpg';            link.href = canvas.toDataURL('image/jpeg');            link.click();        }    },    init: () => {        const canvas = document.getElementById('paint_canvas');        canvas.width = 750;        canvas.height = 450;        const ctx = canvas.getContext('2d');        let drawing = false;        ctx.lineCap = 'round';        canvas.addEventListener('mousedown', () => drawing = true);        canvas.addEventListener('mouseup', () => { drawing = false; ctx.beginPath(); });        canvas.addEventListener('mousemove', (e) => {            if (!drawing) return;            const rect = canvas.getBoundingClientRect();            ctx.lineWidth = document.getElementById('paint_size').value;            ctx.strokeStyle = document.getElementById('paint_color').value;            ctx.lineTo(e.clientX - rect.left, e.clientY - rect.top);            ctx.stroke();            ctx.beginPath();            ctx.moveTo(e.clientX - rect.left, e.clientY - rect.top);        });    }});